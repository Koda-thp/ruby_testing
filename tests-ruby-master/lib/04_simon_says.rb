def echo (string)
    string = string
end
def shout (string)
    string = string.upcase
end
def repeat(text, n=2)
    return ([text] * n * ' ').strip
end
 def start_of_word (text,x)
    text.slice(0,x)
end
def first_word (x,y)


end

















#capitalize = split(string), map(array),join(reconvertir en string)